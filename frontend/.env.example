# Leave blank for local dev (Vite proxies /api to http://localhost:8000, see vite.config.js).
# For a deployed build, set this to your deployed backend's base URL, e.g.:
# VITE_API_BASE=https://pm-target-screener-api.onrender.com
VITE_API_BASE=
