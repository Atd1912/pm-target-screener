[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Set VITE_API_BASE in the Netlify dashboard to your deployed backend URL,
# e.g. https://pm-target-screener-api.onrender.com
