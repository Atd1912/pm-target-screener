# SQLite by default. Swap to Postgres in one line, e.g.:
# DATABASE_URL=postgresql://user:password@host:5432/pm_target_screener
DATABASE_URL=sqlite:///./pm_targets.db

# Required for the /api/query natural-language search endpoint.
ANTHROPIC_API_KEY=
